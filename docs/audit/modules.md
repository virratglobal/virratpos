# Modules Audit

## Installed Modules
- **LandingPage**: Found in the `Modules/` directory. Appears to manage the SaaS marketing/landing page.
- Utilizes `nwidart/laravel-modules`.

## Core Application Structure
- Most business logic is not inside independent modules but resides in the `app/` directory.
